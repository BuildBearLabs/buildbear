{
  "name": "buildbear",
  "version": "0.1.0",
  "description": "BuildBear CLI — manage sandboxes, faucets, snapshots, and contracts from the terminal",
  "main": "dist/index.js",
  "bin": {
    "buildbear": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["buildbear", "sandbox", "blockchain", "cli"],
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0",
    "cli-table3": "^0.6.3",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
